import React from 'react';
// import { useParams } from 'react-router-dom';
// TODO: Implement useGetApplicationByIdQuery in applicationApi
// import { useGetApplicationByIdQuery } from '../../store/api/companyApi';
// import Loader from '../../components/common/Loader';

const ApplicationDetails: React.FC = () => {
  // const { id } = useParams<{ id: string }>();
  // TODO: Implement application details query
  // const { data: app, isLoading } = useGetApplicationByIdQuery(id!);

  // if (isLoading) return <Loader />;

  // const candidate = app?.user;
  // const job = app?.offre;

  return (
    <div className="page-content">
      <h2>Application Details - Coming Soon</h2>
      <p>This feature is under development.</p>
    </div>
  );
};

export default ApplicationDetails;